# Dataset Generation / Setup

The purpose of this stage is to collect all of the input required from the user so the rest of the program can run automatically.

Really, what this does it make sure the `Data/<Dataset-ID>` folder has a valid `experiment.json` and generates the `neurons.txt` list if it doesn't already exist.